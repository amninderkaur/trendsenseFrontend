# FASHIONAPP

capstone Project

## All modules you must install so front end will work

1. npm install
2. npm install expo router
3. npm install nativewind tailwindcss
4. npm install axios
5. npx expo install react-native-svg
6. npm install --save-dev react-native-svg-transformer

To start frontend use command
npx expo start -c
